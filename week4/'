/usr/bin/env python

import sys
import time
import paramiko

MAX_BUFFER = 65536

disable_paging(conn, cmd='terminal length 0')
    '''
    Disable display paging.
    '''
    conn.send_command(cmd + '\n')
    





if __name__ == "__main__":
    main()
